function charSymb(input) {
    let text = input[0];
    let length = text.length;

        for (let i = 0; i < text.length; i++) {
            let letter = text[i];
            console.log(letter);
            
        }
}
charSymb(["softuni"]);